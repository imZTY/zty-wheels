# 公共配置文件

spring:
  profiles:
    # 环境 dev|test|prod
    active: prod
  servlet:
    multipart:
      max-file-size: 300MB
      max-request-size: 320MB

mybatis:
  typeAliasesPackage: com.zty.pay.dao
  mapperLocations: classpath*:mapper/*.xml
  config-location: classpath:mybatis-config.xml

